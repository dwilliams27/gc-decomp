[melee]
repo_path = "/Users/dwilliams/proj/melee-fork/melee"  # same path, bind-mounted
version = "GALE01"

[agent]
max_iterations = 30

[docker]
enabled = false  # We're already inside the build container

[orchestration]
db_path = "/workspace/gc-decomp/decomp.db"

[ghidra]
enabled = false
